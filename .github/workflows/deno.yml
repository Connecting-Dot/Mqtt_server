name: Hello

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: [self-hosted, Intangles_AWS_EC2]

    steps:
      - name: Say Hello
        run: echo Hello
